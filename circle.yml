machine:
  ruby:
    version:
      jruby-9.1.7.0
  java:
    version:
      openjdk7
  environment:
    JRUBY_OPTS: '--dev'
dependencies:
  pre:
    - bundle config github.https true
test:
  override:
    - bundle exec rspec:
        parallel: true
        pwd: app
